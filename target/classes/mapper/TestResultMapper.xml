<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luofeng.facetest.mapper.TestResultMapper">

    <select id="getUserTestResults" parameterType="map" resultType="com.luofeng.facetest.pojo.testResult">
        SELECT 
            id, 
            user_id as userId, 
            CASE 
                WHEN image LIKE 'http%' THEN image
                ELSE CONCAT('https://java-web-ai388.oss-cn-beijing.aliyuncs.com/', image)
            END AS imagePath,
            test_time as testTime, 
            is_gland_face as isGlandFace, 
            CASE 
                WHEN level LIKE '%轻%' OR level LIKE '%微%' THEN '轻度'
                WHEN level LIKE '%中%' OR level LIKE '%等%' THEN '中度'
                WHEN level LIKE '%重%' THEN '重度'
                ELSE level
            END as level, 
            probability as confidence, 
            visualization_description as visualizationDescription
        FROM test_result 
        WHERE user_id = #{userId}
        <if test="startDate != null">
            AND test_time >= #{startDate}
        </if>
        <if test="endDate != null">
            AND test_time <![CDATA[ <= ]]> #{endDate}
        </if>
        <if test="isGlandFace != null">
            AND is_gland_face = #{isGlandFace}
        </if>
        <if test="level != null and level != ''">
            <choose>
                <when test='level == "轻度"'>
                    AND (level LIKE "%轻%" OR level LIKE "%微%")
                </when>
                <when test='level == "中度"'>
                    AND (level LIKE "%中%" OR level LIKE "%等%")
                </when>
                <when test='level == "重度"'>
                    AND level LIKE "%重%"
                </when>
                <otherwise>
                    AND level = #{level}
                </otherwise>
            </choose>
        </if>
        ORDER BY test_time DESC
    </select>
    
    <delete id="deleteUserTestResult">
        DELETE FROM test_result 
        WHERE id = #{resultId} AND user_id = #{userId}
    </delete>
    
    <delete id="deleteBatchUserTestResults">
        DELETE FROM test_result 
        WHERE user_id = #{userId} AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <select id="getTestResultsForExport" parameterType="map" resultType="com.luofeng.facetest.pojo.testResult">
        SELECT 
            id, 
            user_id as userId, 
            CASE 
                WHEN image LIKE 'http%' THEN image
                ELSE CONCAT('https://java-web-ai388.oss-cn-beijing.aliyuncs.com/', image)
            END AS imagePath,
            test_time as testTime, 
            is_gland_face as isGlandFace, 
            CASE 
                WHEN level LIKE '%轻%' OR level LIKE '%微%' THEN '轻度'
                WHEN level LIKE '%中%' OR level LIKE '%等%' THEN '中度'
                WHEN level LIKE '%重%' THEN '重度'
                ELSE level
            END as level, 
            probability as confidence, 
            visualization_description as visualizationDescription
        FROM test_result 
        WHERE user_id = #{userId}
        <if test="startDate != null">
            AND test_time >= #{startDate}
        </if>
        <if test="endDate != null">
            AND test_time <![CDATA[ <= ]]> #{endDate}
        </if>
        <if test="isGlandFace != null">
            AND is_gland_face = #{isGlandFace}
        </if>
        <if test="level != null and level != ''">
            <choose>
                <when test='level == "轻度"'>
                    AND (level LIKE "%轻%" OR level LIKE "%微%")
                </when>
                <when test='level == "中度"'>
                    AND (level LIKE "%中%" OR level LIKE "%等%")
                </when>
                <when test='level == "重度"'>
                    AND level LIKE "%重%"
                </when>
                <otherwise>
                    AND level = #{level}
                </otherwise>
            </choose>
        </if>
        ORDER BY test_time DESC
    </select>
</mapper>